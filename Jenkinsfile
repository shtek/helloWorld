pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
               echo 'ffjfjf'
               // sh 'make'
               // archiveArtifacts artifacts: '**/target/*.jar', fingerprint: true
            }
        }
    }
}